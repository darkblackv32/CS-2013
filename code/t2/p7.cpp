#include "p7.h"

int main(){

    std::array<int, 3> arr = {10, 20, 30};
    std::cout << "Los valores de arr son: " << arr << std::endl; // Los valores de lst son: {10, 20, 30}
    std::cout << arr << std::endl;
    // ? overload << after string 
    // ? << " " << 
}